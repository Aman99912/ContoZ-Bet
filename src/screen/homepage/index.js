import React from 'react';
import { View } from 'react-native';
import CText from '@/components/common/CText';

export default function HomeScreen() {
    return (
        <View>
            <CText>Homepage</CText>
        </View>
    );
}