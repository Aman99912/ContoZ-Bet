import React from 'react';
import { View } from 'react-native';
import CText from '@/components/common/CText';

export default function UserScreen() {
    return (
        <View>
            <CText>ProfileScreen</CText>
        </View>
    );
}