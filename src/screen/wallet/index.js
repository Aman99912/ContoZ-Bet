import React from 'react';
import { View } from 'react-native';
import CText from '@/components/common/CText';

export default function WalletScreen() {
    return (
        <View>
            <CText>walletScreen</CText>
        </View>
    );
}